<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.platform.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.edu.platform.domain.User">
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="name" column="name"/>
        <result property="role" column="role"/>
        <result property="schoolId" column="school_id"/>
        <result property="phone" column="phone"/>
        <result property="profileImgUrl" column="profile_img_url"/>
        <result property="isActive" column="is_active"/>
        <result property="isEmailVerified" column="is_email_verified"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
        <result property="notiAssignment" column="noti_assignment"/>
        <result property="notiAnnouncement" column="noti_announcement"/>
        <result property="notiInquiryReply" column="noti_inquiry_reply"/>
    </resultMap>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE user_id = #{userId} AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users
        WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users
        WHERE deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="role != null and role != ''">
            AND role = #{role}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
        WHERE deleted_at IS NULL
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="role != null and role != ''">
            AND role = #{role}
        </if>
    </select>

    <select id="countByRole" resultType="long">
        SELECT COUNT(*) FROM users WHERE role = #{role} AND deleted_at IS NULL
    </select>

    <select id="countByIsActive" resultType="long">
        SELECT COUNT(*) FROM users WHERE is_active = #{isActive} AND deleted_at IS NULL
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM users WHERE deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (email, password_hash, name, role, school_id, phone, is_active, is_email_verified, created_at, updated_at)
        VALUES (#{email}, #{passwordHash}, #{name}, #{role}, #{schoolId}, #{phone}, #{isActive}, #{isEmailVerified}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE users SET
            name = #{name},
            phone = #{phone},
            profile_img_url = #{profileImgUrl},
            updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateLastLogin">
        UPDATE users SET last_login_at = NOW(), updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateActive">
        UPDATE users SET is_active = #{isActive}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="softDelete">
        UPDATE users SET deleted_at = NOW() WHERE user_id = #{userId}
    </update>

    <update id="updatePassword">
        UPDATE users SET password_hash = #{passwordHash}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateNotificationSettings" parameterType="com.edu.platform.domain.User">
        UPDATE users
        SET noti_assignment    = #{notiAssignment},
            noti_announcement  = #{notiAnnouncement},
            noti_inquiry_reply = #{notiInquiryReply},
            updated_at         = NOW()
        WHERE user_id = #{userId}
    </update>

</mapper>
