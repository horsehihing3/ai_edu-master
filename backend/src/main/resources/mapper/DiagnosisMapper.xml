<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.platform.mapper.DiagnosisMapper">

    <resultMap id="DiagnosisResultMap" type="com.edu.platform.domain.DiagnosisTest">
        <id property="testId" column="test_id"/>
        <result property="studentId" column="student_id"/>
        <result property="status" column="status"/>
        <result property="startedAt" column="started_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="totalQuestions" column="total_questions"/>
        <result property="correctCount" column="correct_count"/>
        <result property="scoreRate" column="score_rate"/>
        <result property="determinedLevel" column="determined_level"/>
        <result property="aiAnalysis" column="ai_analysis"/>
    </resultMap>

    <select id="findById" resultMap="DiagnosisResultMap">
        SELECT * FROM diagnosis_tests WHERE test_id = #{testId}
    </select>

    <select id="findLatestByStudentId" resultMap="DiagnosisResultMap">
        SELECT * FROM diagnosis_tests WHERE student_id = #{studentId}
        ORDER BY started_at DESC
        LIMIT 1
    </select>

    <select id="findInProgressByStudentId" resultMap="DiagnosisResultMap">
        SELECT * FROM diagnosis_tests
        WHERE student_id = #{studentId} AND status = 'IN_PROGRESS'
        ORDER BY started_at DESC
        LIMIT 1
    </select>

    <select id="existsInProgressByStudentId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM diagnosis_tests
        WHERE student_id = #{studentId} AND status = 'IN_PROGRESS'
    </select>

    <update id="abandonInProgressByStudentId">
        UPDATE diagnosis_tests
        SET status = 'ABANDONED'
        WHERE student_id = #{studentId} AND status = 'IN_PROGRESS'
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="testId">
        INSERT INTO diagnosis_tests (student_id, status, started_at, total_questions)
        VALUES (#{studentId}, #{status}, #{startedAt}, #{totalQuestions})
    </insert>

    <update id="update">
        UPDATE diagnosis_tests SET
            status = #{status},
            completed_at = #{completedAt},
            correct_count = #{correctCount},
            score_rate = #{scoreRate},
            determined_level = #{determinedLevel},
            ai_analysis = #{aiAnalysis}
        WHERE test_id = #{testId}
    </update>

</mapper>
